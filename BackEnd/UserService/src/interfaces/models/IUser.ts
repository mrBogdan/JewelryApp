//@firstName, @lastName, @email, @password, @isAdmin, @imageUrl, @address, @phone

export interface IUser {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
    isAdmin: boolean;
    imageUrl: string;
    address: string;
    phone: string;
}